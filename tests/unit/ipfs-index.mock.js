export function upload({ fileChunk, fileName, fileType }){} // eslint-disable-line
export function syncDecryptedFromIPFS(path, pair, fileName, type){} // eslint-disable-line
export function downloadDecryptedFromIPFS(path, secretKey, publicKey, fileName, type){} // eslint-disable-line
export function download(data, fileName){} // eslint-disable-line
export function downloadPDF(data, fileName){} // eslint-disable-line
