module.exports = {
    addUri(provider) {}, // eslint-disable-line
    restoreAccount(provider) {}, // eslint-disable-line
    getPair(provider) {}, // eslint-disable-line
    loadAll(provider) {} // eslint-disable-line
};
